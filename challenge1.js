const totalBasketballScore = (freeThrow, midRange, threePointer) => {
    switch(randomNumber) {
        case freeThrow:
        return 1
        case midRange:
        return 2
        case threePointer:
        return 3
    
    }
};


const totalBasketballScore = function() {

    freethrow = 1;
    midRange = 2;
    threePointer = 3; 
}; 
    